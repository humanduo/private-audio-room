# AI 智能资料库、筛选、收藏与推荐需求文档

> 目标：把 NAS 里已经扫描出来的广播剧、有声书、网课，进一步整理成更好找、更好看、更懂自己口味的私人声音资料库。  
> 实现方式：分批上线，先做可用的基础资料和收藏，再做 AI 识别，最后做相似推荐。

## 1. 背景

现在系统已经能从 NAS 扫描音频、分季展示广播剧、播放分集、显示封面和进度。但广播剧多起来以后，只靠文件夹名和集数不够用，会遇到几个问题：

- 不知道这个剧是什么类型，比如言情、悬疑、恐怖、古风、现代、修仙。
- 不知道配音演员是谁、原著作者是谁、是否完结。
- 首页和详情页介绍不够丰富，看不出“这部剧值不值得听”。
- 收藏以后没有分类，剧多了以后不好找。
- 听到喜欢的作者或相似类型时，不能快速发现同类作品。

这个需求的核心不是做公开社区，也不是做外部内容平台，而是给自己的本地 NAS 内容增加一层“智能整理”和“私人收藏”。

## 2. 总目标

增加以下能力：

1. 专辑资料增强：为广播剧增加简介、配音演员、作者、标签、题材、情感向、完结状态等信息。
2. AI 智能识别：一键分析广播剧标题和文件信息，自动生成标签、简介、配音演员、作者、完结状态等元数据。
3. 高级筛选：支持按题材、感情向、完结状态、标签筛选。
4. 收藏夹：支持收藏广播剧到默认收藏夹或自建收藏夹。
5. 我的收藏搜索：个人主页里能查看收藏夹，并在收藏里搜索。
6. 相似推荐：在广播剧详情页推荐同作者、同配音、同题材、同标签的本地作品。
7. CV 搜索：支持按喜欢的配音演员 / CV 直接查找其参与的广播剧。

## 3. 不做范围

第一阶段不做这些：

- 不做公开评论区。
- 不做粉丝互动、饭圈、动态、社区。
- 不做商城、会员、直播、活动。
- 不自动上传 NAS 文件到外部平台。
- 不默认联网搜索全部资料，除非用户明确点击 AI 识别或联网补全。
- 不把 AI 识别结果当成绝对准确，必须允许手动修改。

## 4. 专辑资料字段

### 4.1 Album 新增字段

后续建议把 `Album` 扩展为：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `author` | `string` | 原著作者或课程主讲人 |
| `cast` | `string[]` | 配音演员、主播、CV |
| `summary` | `string` | 剧情简介或课程简介 |
| `genres` | `string[]` | 题材标签，例如悬疑、言情、恐怖、刑侦、古风 |
| `relationship` | `string` | 感情向，例如言情、耽美、百合、无 CP、群像 |
| `audience` | `string` | 组合筛选，例如男男、女女、男女、群像 |
| `finishStatus` | `string` | 已完结、连载中、未知 |
| `sourceTitle` | `string` | 清理前原始标题，方便回溯 |
| `aiMetaStatus` | `string` | 未识别、已识别、需要确认、识别失败 |
| `aiMetaUpdatedAt` | `string` | 上次 AI 识别时间 |

### 4.2 标签建议

默认题材标签可以先内置这些：

- 感情：言情、耽美、百合、无 CP、群像
- 背景：古代、现代、民国、未来、校园、职场、娱乐圈
- 类型：悬疑、刑侦、恐怖、灵异、修仙、玄幻、无限流、权谋、甜宠、虐恋、治愈、沙雕、强强
- 状态：已完结、连载中、未听、在听、听完

筛选项可以先固定，后续再允许用户自定义。

## 5. AI 智能识别

### 5.1 功能入口

AI 识别入口放在三个地方：

1. 广播剧详情页：按钮 `AI 整理资料`
2. 文件页/系列墙：长按或更多菜单里 `AI 整理`
3. 我的页：`资料整理`，可批量整理缺简介、缺标签的专辑

### 5.2 单个识别流程

用户点击 `AI 整理资料` 后：

1. 后端读取专辑标题、清理前文件夹名、清理后剧名、分季信息、已有标签、已有简介。
2. 后端根据文件名 / 剧名进行联网搜索，优先查找公开资料页、官方节目页、百科、豆瓣、猫耳 / 漫播 / 饭角等公开可访问信息。
3. 后端把搜索摘要和来源链接交给便宜模型，例如 DeepSeek。
4. AI 返回结构化 JSON。
5. 后端校验字段，不合格就丢弃或标记为需要确认。
6. 前端显示确认弹窗：
   - 剧情简介
   - 作者
   - 配音演员
   - 题材标签
   - 感情向
   - 完结状态
7. 用户点击保存后写入本地数据。

识别原则：

- 以专辑文件夹名 / 剧名为主要搜索依据。
- 不根据分集标题强行推断整部剧资料，分集标题只作为辅助信息，避免把单集剧情误判成整部剧标签。
- 配音演员信息不确定时，优先通过联网搜索确认；如果仍然不确定，可以保存但必须标记为 `需要确认`。
- 资料来源要保存到本地，方便用户以后回看和修正。

### 5.3 批量识别流程

批量识别分两种模式：

1. 智能补全：只处理缺资料的专辑，避免浪费钱。
2. 全库重整：重新分析所有广播剧，用于第一次整理 NAS 内容，或用户觉得标签体系需要重做时。

智能补全处理这些专辑：

- 缺 `summary`
- 缺 `genres`
- 缺 `cast`
- `aiMetaStatus = 未识别`

全库重整处理所有广播剧，但要保护用户手动修改过的字段：

- 用户手动改过的简介、标签、作者、配音演员默认不覆盖。
- 如果用户选择 `允许覆盖`，AI 可以重新生成全部字段。
- 全库重整完成后，需要展示“已更新 / 需要确认 / 失败”的数量。
- 每个专辑保存 `aiMetaStatus`，方便下次只处理失败或待确认项目。

批量执行时要有上限：

- 每次最多 10 个或 20 个。
- 显示本次预计调用数量。
- 每个失败不影响其他专辑。
- 失败原因记录到本地。

### 5.4 DeepSeek 全库智能分析

最终目标是让 DeepSeek 成为本地广播剧资料库的“整理助手”。它需要分析所有广播剧，并生成能直接用于首页展示、筛选和推荐的数据。

分析输入：

- 专辑标题
- 原始文件夹名
- 分季信息
- 已有标签
- 已有简介
- 已有作者和配音演员
- 联网搜索得到的公开资料摘要
- 联网搜索来源链接

分集标题列表不作为主要分析输入，只在缺少公开资料时作为弱辅助，避免根据分集名误推整部作品。

分析输出：

- 清理后的剧名
- 原著作者
- 配音演员或主要主播
- 剧情简介
- 短简介，供首页卡片使用
- 题材标签
- 感情向
- 主角组合
- 完结状态
- 推荐关键词
- 相似推荐依据，例如同作者、同题材、同配音

DeepSeek 结果会进入三个系统：

1. 首页介绍：显示短简介、标签、作者或主要配音。
2. 筛选系统：使用 `genres`、`relationship`、`audience`、`finishStatus`。
3. 推荐系统：使用作者、配音、标签和推荐关键词计算相似作品。

全库分析入口：

- 我的页增加 `AI 整理全库`
- 文件页增加 `整理所有广播剧`
- 设置里增加 `AI 资料整理`

全库分析状态：

- 未开始
- 分析中
- 进度展示：总数、已完成、成功、失败、当前正在整理的广播剧
- 等待确认
- 已完成
- 部分失败

全库分析不能静默覆盖所有结果。第一次做完后，前端要给一个确认页：

- 可以按专辑逐个确认。
- 可以批量接受高置信度结果。
- 低置信度结果标记为 `需要确认`。
- 用户可以跳过某些专辑。

第一版确认体验：

- DeepSeek 整理后先保存资料，但如果 `needsReview=true`，专辑状态标记为 `待确认`。
- 我的页增加 `AI 待确认` 列表，展示需要用户检查的广播剧。
- 用户点进广播剧详情后，可以查看作者、配音、简介、标签，并通过 `编辑资料` 手动修正。
- 用户保存资料后，状态从 `待确认` 变成 `AI 资料已保存`。

### 5.5 DeepSeek 配置

后端环境变量建议：

```text
AI_METADATA_PROVIDER=deepseek
DEEPSEEK_API_KEY=你的 key
DEEPSEEK_MODEL=deepseek-chat
AI_METADATA_BATCH_LIMIT=10
AI_METADATA_ALLOW_WEB_SEARCH=true
AI_METADATA_SEARCH_SOURCE=filename
```

如果以后要切换 OpenAI、通义千问、智谱等，只需要换 provider。

默认允许联网搜索，但搜索关键词来自文件夹名 / 剧名，不直接用分集名推断作品资料。联网搜索只用于补全元数据，不用于推荐外部作品。

### 5.6 AI 输出格式

AI 必须返回 JSON，不要返回自然语言长文。

```json
{
  "title": "今日离港",
  "author": "鱼宰",
  "cast": ["风镜", "陶典"],
  "summary": "一段围绕离别、重逢和自我选择展开的现代情感广播剧。",
  "genres": ["现代", "言情", "治愈"],
  "relationship": "男女",
  "audience": "男女",
  "finishStatus": "已完结",
  "shortSummary": "现代情感广播剧，围绕离别和重逢展开。",
  "recommendationKeywords": ["现代言情", "治愈", "离别重逢"],
  "confidence": 0.72,
  "needsReview": true
}
```

### 5.7 风险控制

- AI 可能胡编配音演员，所以默认标记为 `需要确认`。
- 资料来源不明确时，简介可以保存，配音和作者要降低可信度。
- 用户手动改过的资料，AI 不应直接覆盖。
- 批量识别时要可取消。
- AI key 只保存在 NAS 后端环境变量，前端不能暴露。
- 全库分析必须分批执行，不能一次性把几百部剧全部塞进一个请求。
- 全库分析要有任务进度，刷新页面后还能看到状态。
- 如果 DeepSeek 返回不合法 JSON，后端要重试一次，仍失败就标记为识别失败。

## 6. 筛选系统

### 6.1 首页筛选

首页广播剧列表增加多层筛选：

1. 基础分类：全部、古代、现代、修仙
2. 感情向：全部、言情、耽美、百合、无 CP、群像
3. 主角组合：全部、男女、男男、女女、群像
4. 完结状态：全部、已完结、连载中、未知
5. 题材类型：悬疑、恐怖、刑侦、甜宠、虐恋、权谋、无限流、校园、娱乐圈

移动端不要一次铺太多筛选项。建议：

- 首页只展示一行常用筛选。
- 点 `筛选` 打开底部弹窗。
- 弹窗里分组展示：感情向、状态、题材、标签。

### 6.2 筛选逻辑

- 同一组内单选，例如完结状态只能选一个。
- 题材标签可以多选，例如悬疑 + 刑侦。
- 多组之间为 AND，例如 `已完结 + 悬疑 + 现代`。
- 搜索关键词和筛选同时生效。

### 6.3 CV 搜索

增加一个专门面向配音演员 / CV 的搜索能力，方便用户直接找自己喜欢的 CV 参与的广播剧。

入口建议：

- 首页顶部搜索框旁增加一个轻量入口：`搜 CV`。
- 搜索页增加 `CV` 筛选标签，和普通剧名搜索区分。
- 详情页配音演员名字可点击，点击后进入该 CV 的作品列表。

搜索逻辑：

- 优先匹配 `cast` 字段。
- 兼容别名、大小写和中英文空格，例如 `景向谁依`、`景向 谁依`。
- 如果 DeepSeek 已识别配音演员，直接使用 AI 保存后的 `cast`。
- 如果还没有整理资料，可以从文件夹名、专辑简介、标签里做弱匹配，但需要标记为“不确定”。

展示方式：

- 搜索结果页标题显示：`景向谁依 参与的广播剧`。
- 结果卡片显示该 CV 在当前广播剧中的命中来源，例如 `配音演员`、`简介提到`、`待确认`。
- 可以继续叠加题材、完结状态、感情向筛选。

后续增强：

- 支持把喜欢的 CV 收藏到 `我的 CV`。
- 我的页增加 `喜欢的 CV` 入口。
- 某个 CV 有新扫描出的本地作品时，在我的页提示。

## 7. 收藏夹

### 7.1 收藏入口

广播剧卡片和详情页增加收藏按钮：

- 未收藏：空心爱心或收藏图标
- 已收藏：实心状态

点击收藏时弹出收藏弹窗：

1. 默认选中 `默认收藏夹`
2. 显示已有收藏夹列表
3. 有 `新建收藏夹` 按钮
4. 可以输入收藏夹名称，例如言情、悬疑、睡前听、朋友推荐
5. 保存后把当前广播剧加入对应收藏夹

### 7.2 收藏夹数据结构

建议新增：

```ts
type FavoriteFolder = {
  id: string;
  name: string;
  albumIds: string[];
  createdAt: string;
  updatedAt: string;
};
```

默认系统自动创建：

- 默认收藏夹

### 7.3 我的页收藏

个人主页增加 `我的收藏`：

- 顶部搜索框：只搜索收藏里的广播剧。
- 收藏夹横向列表：全部、默认收藏夹、言情、悬疑、睡前听等。
- 下方展示收藏内容。
- 收藏内容支持按最近收藏、最近播放、标题排序。

### 7.4 小红书式收藏体验

参考小红书收藏，不做社交，只借交互：

- 点击收藏后弹出底部收藏夹选择。
- 默认收藏到最近使用的收藏夹。
- 支持新建收藏夹。
- 支持取消收藏。
- 支持从某个收藏夹移除，但不删除专辑本身。

## 8. 相似推荐

### 8.1 详情页推荐区域

在广播剧详情页底部增加：

标题：`你可能还想听`

推荐来源：

1. 同作者，例如墨宝非宝的其他广播剧。
2. 同配音演员，例如同 CV 参与的作品。
3. 同题材，例如现代言情、悬疑刑侦。
4. 同收藏夹，例如都被放进“睡前听”。

### 8.2 推荐排序

推荐分数可以先用简单规则：

| 命中项 | 分数 |
| --- | --- |
| 同作者 | +5 |
| 同配音演员 | 每个 +3 |
| 同感情向 | +3 |
| 同题材标签 | 每个 +2 |
| 同完结状态 | +1 |
| 已经听完 | -2 |
| 当前专辑 | 排除 |

第一版不需要复杂算法，本地规则就够用。

### 8.3 推荐卡片

推荐卡片显示：

- 封面
- 标题
- 作者
- 共同标签，例如 `同作者 · 现代言情`
- 播放按钮

点击后进入该广播剧详情。

## 9. 页面改动

### 9.1 首页

广播剧卡片新增：

- 简短简介，最多 1 行或 2 行。
- 标签胶囊，例如 `悬疑` `现代` `已完结`。
- 当前播放集数。
- 收藏状态。

首页顶部 hero 新增：

- 当前集数文案。
- 主要标签，例如 `现代 · 悬疑 · 已完结`。
- 作者或主要配音演员，可在空间允许时显示。

### 9.2 详情页

详情页新增信息区：

- 剧情简介
- 作者
- 配音演员
- 标签
- 完结状态
- 收藏按钮
- AI 整理资料按钮
- 你可能还想听

### 9.3 我的页

我的页新增：

- 我的收藏
- 收藏搜索
- 收藏夹管理
- AI 资料整理入口

## 10. 后端 API 草案

### 10.1 AI 识别资料

```http
POST /api/albums/:id/metadata/analyze
```

返回 AI 建议，不直接覆盖：

```json
{
  "metadata": {
    "author": "鱼宰",
    "cast": ["风镜", "陶典"],
    "summary": "...",
    "genres": ["现代", "言情"],
    "relationship": "男女",
    "finishStatus": "已完结"
  }
}
```

### 10.2 保存资料

```http
PATCH /api/albums/:id/metadata
```

### 10.3 批量 AI 识别

```http
POST /api/metadata/analyze-batch
```

请求体：

```json
{
  "mode": "missing-only",
  "kind": "drama",
  "limit": 10,
  "overwriteManualFields": false
}
```

其中 `mode` 支持：

- `missing-only`：只整理缺资料的广播剧。
- `all`：整理所有广播剧。
- `failed-only`：只重试识别失败的广播剧。

### 10.4 全库 AI 分析任务

```http
POST /api/metadata/analyze-library
GET /api/metadata/analyze-library/:jobId
POST /api/metadata/analyze-library/:jobId/apply
POST /api/metadata/analyze-library/:jobId/cancel
```

说明：

- `analyze-library` 创建全库分析任务。
- `GET` 查询任务进度。
- `apply` 保存用户确认后的结果。
- `cancel` 取消任务。

### 10.5 收藏夹

```http
GET /api/favorite-folders
POST /api/favorite-folders
PATCH /api/favorite-folders/:id
DELETE /api/favorite-folders/:id
POST /api/favorite-folders/:id/albums/:albumId
DELETE /api/favorite-folders/:id/albums/:albumId
```

### 10.6 推荐

```http
GET /api/albums/:id/recommendations
```

## 11. 分批实现计划

### 第一批：资料字段和手动编辑

目标：不用 AI，先把数据结构和 UI 打通。

- 扩展 Album 字段：作者、配音演员、简介、题材、感情向、完结状态。
- 详情页显示这些字段。
- 首页卡片显示标签和简介。
- 增加手动编辑资料弹窗。
- 保存到本地 `data/state.json`。

验收：

- 能手动给一个广播剧添加简介、作者、配音演员、标签。
- 刷新页面后资料还在。
- 首页能看到标签和简介。

### 第二批：筛选系统

目标：剧多了以后能快速找到。

- 首页增加筛选入口。
- 支持感情向、主角组合、完结状态、题材类型。
- 搜索和筛选可以同时生效。

验收：

- 选择 `已完结 + 悬疑` 后，只显示符合条件的广播剧。
- 清空筛选后恢复全部。

### 第三批：收藏夹

目标：能像小红书一样收藏到不同收藏夹。

- 新增收藏夹数据结构。
- 详情页和卡片增加收藏按钮。
- 收藏弹窗支持选择默认收藏夹和新建收藏夹。
- 我的页增加我的收藏、收藏夹列表、收藏搜索。

验收：

- 一个广播剧可以收藏到 `默认收藏夹` 或自定义收藏夹。
- 我的页能按收藏夹查看。
- 收藏搜索能搜到已收藏广播剧。

### 第四批：AI 智能识别

目标：用 DeepSeek 一键补全资料，并支持全库整理所有广播剧。

- 接入 DeepSeek。
- 接入联网搜索能力，按文件名 / 剧名查找公开资料。
- 新增 AI 识别接口。
- 单个广播剧 AI 整理资料。
- AI 结果确认弹窗。
- 支持批量识别缺资料专辑。
- 支持 `AI 整理全库`，分析所有广播剧。
- AI 结果进入首页介绍、筛选条件和推荐系统。
- 支持任务进度、取消、失败重试。
- `一键编辑` 开始前先确认整理模式和预计数量。
- 我的页展示整理进度：当前剧名、总数、已完成、成功数、失败数。
- 我的页展示最近任务结果列表，区分成功、待确认和失败。
- 我的页展示 `AI 待确认`，方便用户逐个检查 AI 生成的作者、配音和简介。

验收：

- 点击 `AI 整理资料` 后，根据文件名 / 剧名联网搜索并生成简介、标签、配音演员建议。
- 不根据分集标题强行推断整部剧资料。
- 配音演员不确定时，搜索公开资料确认后再保存；仍不确定则标记为需要确认。
- 点击 `AI 整理全库` 后能分批分析所有广播剧。
- 开始前可以选择 `只整理缺资料`、`重试失败`、`重新整理全部`。
- 开始前能看到本次预计处理多少部，避免误触全库整理。
- 整理过程中能看到当前正在处理哪部广播剧，以及整体百分比。
- 全库分析结束后能看到已更新、待确认、失败数量。
- 失败任务可以一键重试，任务结果可以回看最近处理的广播剧。
- `needsReview=true` 的专辑会出现在我的页 `AI 待确认`。
- 首页能展示 AI 生成的标签和短简介。
- 筛选能使用 AI 生成的题材、感情向、完结状态。
- 用户确认后才保存。
- DeepSeek API key 不暴露到前端。

### 第五批：相似推荐

目标：听完一部以后能发现同作者、同配音、同题材的本地作品。

- 后端增加推荐计算。
- 详情页底部显示 `你可能还想听`。
- 推荐理由显示为 `同作者`、`同配音`、`同题材`。

验收：

- 打开某个广播剧详情页，底部能看到相关广播剧。
- 当前广播剧不会推荐自己。
- 已经没有资料的剧不会乱推荐。

### 第六批：CV 搜索与 CV 收藏

目标：用户可以按喜欢的配音演员 / CV 找到本地已有广播剧。

- 搜索页增加 `CV` 搜索模式。
- 首页搜索入口支持切换到 `搜 CV`。
- 详情页配音演员名字可点击，进入该 CV 的作品列表。
- 搜索结果支持继续按题材、完结状态、感情向筛选。
- 我的页预留 `喜欢的 CV` 区域，后续可收藏常听 CV。

验收：

- 输入一个 CV 名字后，只展示 `cast` 中包含该 CV 的广播剧。
- 点击详情页里的 CV 名字，可以跳到这个 CV 的广播剧列表。
- 没有 AI 资料的广播剧不会被强行命中，除非命中来自文件名或简介，并标记为待确认。
- CV 搜索不需要联网，只搜索本地已整理资料。

## 12. 推荐优先级

建议先做：

1. 第一批：资料字段和手动编辑
2. 第三批：收藏夹
3. 第二批：筛选系统
4. 第四批：AI 智能识别
5. 第五批：相似推荐
6. 第六批：CV 搜索与 CV 收藏

原因：

- 资料字段是所有后续功能的地基。
- 收藏夹对日常使用最直接，马上能提升体验。
- 筛选依赖标签和状态。
- AI 识别要等字段稳定后再接。
- 推荐依赖作者、配音、标签足够丰富。
- CV 搜索依赖 `cast` 字段，适合在 DeepSeek 整理资料后做，命中会更准。

## 13. 后续问题

已确认规则：

1. AI 识别允许联网搜索。
2. 联网搜索根据文件名 / 剧名进行，不根据分集标题推断整部剧资料。
3. 配音演员信息不确定时，用搜索结果确认；如果仍然不确定，可以保存但标记为需要确认。
4. 推荐只推荐本地已有作品，不跳转互联网作品。
5. 网课和有声书暂时不做同样的 AI 资料识别，第一版只做广播剧。

默认建议：

- 推荐只基于本地已有内容。
- AI 结果默认需要确认，尤其是配音演员和作者。
- 第一版只做广播剧，稳定后再考虑扩展到有声书和网课。
